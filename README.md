# Scan QR codes offline / on http://localhost

This Nodejs project uses the webcam to scan QR codes.

Currently, this only runs with Firefox.

Wraps https://www.npmjs.com/package/html5-qrcode to run on localhost.
